*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Assinatura do release — NUNCA versionar (perder a chave = não dá
# mais pra atualizar o app com a mesma identidade).
keystore.properties
*.jks
*.keystore

# Saída do "Generate Signed APK" do Android Studio (APK + cópia do keystore)
app/release/

# Relatórios e turnos exportados pelo app (contêm dados financeiros reais)
*.pdf
